<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.java.boot.base.mapper.PermissionMapper">

    <select id="findPermissionByUser" resultType="com.java.boot.base.model.Permission">
	SELECT
		*
	FROM
		permission
	WHERE
		pid IN ( SELECT permission_id FROM role_permission WHERE role_id IN ( SELECT role_id FROM user_role WHERE uid = #{uid} ) )
    </select>
</mapper>
